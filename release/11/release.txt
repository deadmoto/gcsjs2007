* исправлена ошибка при создании dsn записи:
	-Удалить в ключе реестра [HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\MSSQLServer\Client\ConnectTo] все записи.

add* появились два поля "Адрес","Телефон" в таблице Dist. Нужно для автоматической подстановки в отчетах. Редактирование осуществляется через "Справочники->Округа" (Dist.sql в папке scripts)

* В отчетах поменялась только "шапка" документа (если в отчеты были внесены ваши изменения, то можно взять только "шапку" из новых)